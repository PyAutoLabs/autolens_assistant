benchmark: bootstrap-smoke
prompt_version: 1
date: '2026-09-26'
model: claude-opus-5-5
harness: claude-code
assistant_sha: f690a31
stack:
  autolens: 2026.9.19.1 (installed by the agent from PyPI into <clone>/.venv, Python
    3.12)
  autofit: 2026.9.19.1
  autoarray: 2026.9.19.1
  autonerves: 2026.9.19.1
hardware: WSL2 laptop (Linux 5.10), CPU
operator: Claude Opus 5.5 subagent (headless run, human-authorised)
run:
  duration_minutes: 3.9
  cost_usd: 0.667
  tokens:
    input: 32
    cache_creation: 52573
    cache_read: 602361
    output: 6295
  turns: 16
status: complete
score:
  machine: 50.0
  judged: 47.0
  total: 97.0
  judge: Claude Opus 5.5 subagent (operator), from transcript.jsonl and the files
    the agent left in its directory
notes: "Pre-merge stage (a) of #138: the prompt is the public starting prompt byte\
  \ for byte except the repository URL, replaced by the branch tree URL https://github.com/PyAutoLabs/autolens_assistant/tree/feature/self-bootstrapping-greeting\
  \ so the fetched text is the branch's, standing in for main until merge. Launched\
  \ from an EMPTY directory under the session scratchpad in a scrubbed environment\
  \ (env -i HOME USER LANG TERM PATH=~/.local/bin:node:/usr/local/bin:/usr/bin:/bin;\
  \ no VIRTUAL_ENV, no PYTHONPATH), so no PyAutoLens was importable: system python3\
  \ is 3.10 without pip; python3.9-3.14 present. Command: claude -p <prompt> --output-format\
  \ stream-json --verbose --permission-mode bypassPermissions --max-turns 60, claude-code\
  \ 2.1.283, default model (claude-opus-5-5). Wall 233 s. The agent did NOT fetch\
  \ the URL (web_fetch_requests 0): it listed the empty cwd, inferred the clone from\
  \ the URL itself (git clone -b feature/self-bootstrapping-greeting), then cat'd\
  \ CLAUDE.md/AGENTS.md and the greeting skill (whose Step 0 narration it followed\
  \ nearly verbatim). Install path: audit_skill_apis.py --check-version -> exit 127\
  \ (no `python`), probed ~/venv/PyAuto read-only (exit 2, NOT INSTALLED, no install\
  \ into it), read skills/al_setup_environment.md, built <clone>/.venv with python3.11\
  \ -> pip install 'autolens[jax]' numba FAILED (autolens now requires Python >=3.12;\
  \ the skill still says 3.11 \u2014 follow-up), rebuilt with python3.12 -> installed\
  \ 2026.9.19.1, --check-version exit 0 (drift clean). Workspace version warning seen:\
  \ repo records 2026.7.9.1, installed 2026.9.19.1 (>30 days); agent set PYAUTO_SKIP_WORKSPACE_VERSION_CHECK=1.\
  \ Code gate self-enforced: audit_skill_apis.py --file plot_dataset.py -> audit=0\
  \ before running. Writes: only inside its empty directory (plus pip's own cache)."
