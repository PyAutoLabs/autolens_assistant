# Benchmark results

Regenerated by `python autoassistant/benchmark.py report` — do not edit by hand. Last regenerated: 2026-09-17.

Scores are comparable only within the same benchmark **and** prompt version; judged sub-scores are comparable only across runs graded by the same judge.

The four 2026-07 conversational cards are retired under `prompts/conversational/` and are not listed — they need an operator and cannot produce comparable scores.

## One-shot benchmarks

Headless runs, computed scores: every gate must pass, each metric reads 0–1, and `score = 100 × mean(metrics)` — zero if any gate fails. Compare only within one prompt version; the wall/compute columns are secondary (same-hardware) readings, not part of the score.

### oneshot-smoke

_No runs recorded yet._

## harness-smoke

_No runs recorded yet._
